# Environment & Secrets
.env
.env.local
.env.*.local
!.env.example

# Database — keep impacthub.db committed (organisation-internal snapshot),
# but never track the SQLite WAL/SHM sidecar files (they change every second).
*.db-shm
*.db-wal

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Node
node_modules/
dist/
dist-ssr/

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code session state
.claude/

# Honor enrichment local config & outputs (config holds API key)
pipeline/data/honor_enrich/config.yaml
pipeline/data/honor_enrich/output/

# WeChat MP login cookie (sensitive — re-extract via DevTools when expired)
pipeline/crawl/mentions_collect/.mp_cookie.json
